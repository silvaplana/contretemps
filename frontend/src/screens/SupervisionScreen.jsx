// Onglet "Supervision" (demande du 2026-09-21) : réservé au Super User,
// au-dessus de toutes les écoles (voir spec/SPEC.md §2.5). Vide pour
// l'instant — son contenu reste à définir.
export default function SupervisionScreen() {
  return (
    <div className="screen">
      <p className="muted">Rien ici pour l’instant.</p>
    </div>
  )
}
